<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('style.ejs') %> 
</head>
<body>
    <div class="mx-4 my-2">
        <p class="text-6xl font-semibold text-center">Nuclear Data</p>
        <p class="text-2xl text-center">Mapping project by KingCode</p>
        <form class="mx-auto bg-white px-6 py-4 rounded mt-5 shadow-2xl shadow-stone-400 w-min hover:-translate-y-2 ease-in-out transition duration-300" action="/country" method="post">
            <label class="text-center block font-medium text-xl align-baseline" for="">Search by Country:</label>
            <input class="mt-2 mx-auto border px-2 py-2 rounded align-baseline" name="country" type="text">
        </form>
        <p class="text-center text-4xl mt-12 font-semibold">Tables:</p>
        <% locals.tables.forEach((data, i) => { %>
            <div onclick="location.href = '/table/<%= i + 1%>'" class="mt-12 w-1/2 mx-auto border-b-2 pb-4">
                <p class="text-3xl font-semibold line-clamp-1"><%= data.title %>:</p>
                <p class="text-lg line-clamp-3 mt-2"><%= data.description %></p>
            </div>
        <% }) %> 
        <!-- <pre><%= JSON.stringify(locals.tables, undefined, 2) %> </pre> -->
    </div>
</body>
</html>